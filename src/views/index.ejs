<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f1f1f1;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #333;
            color: white;
            padding: 15px 30px;
            border-bottom: 2px solid #555;
        }

        .header img {
            height: 100px;
            width: auto;
        }

        .saldo-container {
            background-color: transparent;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 18px;
            color: white;
            font-weight: bold;
        }

        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
        }

        .foto-container {
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
            margin-top: 40px;
            position: relative;
        }

        .patrocinado {
            font-size: 14px;
            color: gray;
            position: absolute;
            top: 10px;
            left: 10px;
        }

        .foto-container img,
        .foto-container video {
            width: 100%;
            max-width: 400px; /* Ajuste para imagens verticais */
            height: 600px; /* Ajuste para que fiquem verticais */
            object-fit: cover;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        .pergunta {
            margin-top: 20px;
            font-size: 18px;
            text-align: center;
            color: #333;
        }

        .avaliacao-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .avaliacao-buttons button {
            padding: 10px 15px;
            border: 2px solid transparent;
            border-radius: 30px;
            font-size: 16px;
            cursor: pointer;
            transition: border-color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .avaliacao-buttons .otima {
            background-color: #FF4081;
            color: white;
        }

        .avaliacao-buttons .media {
            background-color: #FFD700;
            color: black;
        }

        .avaliacao-buttons .pouca {
            background-color: #8B0000;
            color: white;
        }

        .avaliacao-buttons button:hover {
            opacity: 0.8;
        }

        /* Popup 1 (Quanto ganhou) */
        .popup-1 {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 20px;
            border-radius: 10px;
            font-size: 18px;
            text-align: center;
            z-index: 999;
        }

        /* Popup 2 (Vídeo) */
        .popup-2 {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 20px;
            border-radius: 10px;
            font-size: 18px;
            text-align: center;
            z-index: 999;
        }

        /* Estilo de responsividade */
        @media (max-width: 768px) {
            .foto-container img,
            .foto-container video {
                max-width: 90%;
                height: auto;
            }
        }
    </style>
</head>
<body>

    <!-- Cabeçalho -->
    <div class="header">
        <div class="header-container">
            <img src="/images/logo.png" alt="Logo OnlyCash" />
            <div class="saldo-container">
                <p>R$ <span id="saldo">193.59</span></p>
            </div>
        </div>
    </div>

    <!-- Foto ou Vídeo para avaliação -->
    <div class="foto-container">
        <p class="patrocinado">Patrocinado</p>
        <img id="avaliacao-img" src="/images/foto1.jpg" alt="Foto de Avaliação" />
        <video id="avaliacao-video" autoplay loop style="display:none;">
            <source src="/videos/video1.mp4" type="video/mp4">
        </video>

        <p class="pergunta" id="pergunta">Qual o nível de safadeza dessa foto?</p>

        <div class="avaliacao-buttons">
            <button class="otima" onclick="avaliarFoto(1, 'Ótima')">❤️ Ótima</button>
            <button class="media" onclick="avaliarFoto(1, 'Média')">💎 Média</button>
            <button class="pouca" onclick="avaliarFoto(1, 'Pouca')">💍 Pouca</button>
        </div>
    </div>

    <!-- Popup 1 (Quanto ganhou) -->
    <div id="popup-1" class="popup-1">
        Você ganhou R$ <span id="valor-ganho">0.00</span> para começar!
        <br><br>
        
    </div>

    <!-- Popup 2 (Baixar aplicativo) -->
    <div id="popup-2" class="popup-2">
        Baixe o aplicativo e saque seu saldo!
        <br><br>
        <span id="total-saldo">Total R$ 0.00</span>
        <br><br>
        Veja o vídeo de 2 minutos e saque para sua conta bancária!
        <br><br>
        <button onclick="verVideo()">VER VÍDEO</button>
    </div>

    <script>
        let currentPhotoIndex = 1;
        let saldoAtual = 193.59; // Saldo inicial
        const photos = ["/images/foto1.jpg", "/images/foto2.jpg", "/images/foto3.jpg"]; // Lista de fotos
        const videos = ["/videos/video1.mp4", "/videos/video2.mp4"]; // Lista de vídeos
        const perguntas = ["Qual o nível de safadeza dessa foto?", "Você gostaria de me ver todo dia assim?", "Pagaria para ver conteúdos assim?"]; // Perguntas baseadas na foto
        let avaliaçõesFeitas = 0; // Contador para limitar a 3 avaliações

        function avaliarFoto(fotoId, nivelDeSafadeza) {
            if (avaliaçõesFeitas >= 3) {
                alert("Você já completou todas as avaliações!");
                return;
            }

            const valorAvaliado = (Math.random() * (55 - 25) + 25).toFixed(2); // Gera um valor entre 25 e 55 reais

            saldoAtual += parseFloat(valorAvaliado);
            document.getElementById("saldo").textContent = saldoAtual.toFixed(2);
            document.getElementById("valor-ganho").textContent = valorAvaliado;  // Exibe o valor ganho

            // Exibe o popup de quanto ganhou
            document.getElementById("popup-1").style.display = "block";
            setTimeout(() => {
                document.getElementById("popup-1").style.display = "none";
            }, 2000);

            avaliaçõesFeitas++; // Incrementa a contagem de avaliações feitas

            nextPhoto(); // Atualiza para a próxima foto/vídeo
        }

        function nextPhoto() {
            if (avaliaçõesFeitas >= 3) {
                document.getElementById("popup-2").style.display = "block";
                document.getElementById("total-saldo").textContent = `Total R$ ${saldoAtual.toFixed(2)}`;
                return;
            }

            currentPhotoIndex++;
            if (currentPhotoIndex >= photos.length + videos.length) {
                currentPhotoIndex = 0;  // Se passar da última, volta para a primeira foto
            }

            // Se for vídeo, exibe o vídeo
            if (currentPhotoIndex % 2 === 0) {
                const videoElement = document.getElementById('avaliacao-video');
                videoElement.style.display = 'block'; // Exibe o vídeo
                videoElement.src = videos[Math.floor(Math.random() * videos.length)]; // Sorteia um vídeo aleatório

                document.getElementById('avaliacao-img').style.display = 'none'; // Esconde a imagem
            } else {
                const imgElement = document.getElementById('avaliacao-img');
                imgElement.style.display = 'block'; // Exibe a imagem
                imgElement.src = photos[currentPhotoIndex % photos.length]; // Exibe a foto
                document.getElementById('avaliacao-video').style.display = 'none'; // Esconde o vídeo
            }

            // Muda a pergunta conforme a foto
            document.getElementById("pergunta").textContent = perguntas[currentPhotoIndex % perguntas.length];
        }

        function verVideo() {
            window.location.href = '/videos/VSL.mp4'; // Redireciona para o vídeo
        }

        function nextStep() {
            nextPhoto(); // Passa para a próxima avaliação ou encerra
        }
    </script>

</body>
</html>
